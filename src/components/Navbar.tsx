import React from 'react';

const Navbar = () => {
    return (
        <nav>
            <h1>Inventory Management</h1>
            {/* Navbar content */}
        </nav>
    );
};

export default Navbar;
